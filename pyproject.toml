# Project tooling configuration.
#
# The pytest section below puts the `src/` directory on the import path so tests
# can `import app...` without installing the package, and points pytest at the
# `tests/` folder.
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-v"
